export default function ProfileSettings() {
    return (
        <div className="min-h-screen p-8 pt-24">
            <div className="max-w-3xl mx-auto">
                <h1 className="text-3xl font-bold text-white mb-2">Profile Settings</h1>
                <p className="text-[var(--text-muted)] mb-8">Update your personal information and resume.</p>
                <div className="p-8 rounded-xl border border-[var(--border-subtle)] bg-[var(--bg-surface)]">
                    <p className="text-[var(--text-muted)]">Profile edit form (name, bio, photo, resume upload) will be built here.</p>
                </div>
            </div>
        </div>
    );
}
